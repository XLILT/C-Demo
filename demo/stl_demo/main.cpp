#include <stdio.h>
#include <vector>
#include <string>
#include <list>

using namespace std;

int main(int argc, char **argv)
{
    //vector<int> vec;
    //vec.reserve(1024 * 2);
    //vec.push_back(1);

    //list<int> L;
    //L.push_back(1);

    static const string s = "123";

    getchar();

    return 0;
}
